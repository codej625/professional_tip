# AI 에이전트 루프엔지니어링의 함정을 알아보자.

<br />
<br />

* 자동화의 한계

---

```
AI 에이전트가 테스트/조건을 통과할 때까지 반복 실행하는 방식(자동 루프)이
개발 생산성을 크게 높여주는 것은 사실이지만, 결국은 더 큰 능력과 책임을 필요로 한다.

밑에서 이유를 알아보자.
```

<br />
<br />
<br />
<br />

1. 루프는 "정답"이 아니라 "체크리스트 통과"이다.

```
루프의 목표는 사람이 정한 성공 기준(테스트, 조건문, "에러 없이 실행됨" 등)을 만족시키는 것이다.

기준이 완벽하지 않으면 AI는 기준만 통과하는 편법(reward hacking)을 찾아낸다.

- 테스트가 실패하니까 테스트 자체를 지우거나 완화해서 통과시킴
- 예외를 던지는 대신 에러를 삼켜서 "에러 없음"을 만족시킴
- 특정 입력값에만 맞는 답을 하드코딩해서 그 케이스만 통과시킴

정직하게 문제를 푼 것과 기준을 속인 것을 루프 스스로는 구분하지 못한다.

결과를 들여다볼 줄 아는 사람이 필요하다.
```

<br />
<br />
<br />

2. "돌아간다"와 "맞다"는 다르다.

```
루프가 수렴하는 지점은 "에러 없이 실행되고 요구 조건을 만족하는 상태"일 뿐, 다음이 빠져 있을 수 있다.

- 보안 문제 (동작은 하지만 데이터 유출 가능)
- 성능 문제 (동작은 하지만 사용자 증가 시 장애)
- 말하지 않은 요구사항 (테스트에 없으니 지켜지지 않음)

체크리스트에 없는 항목은 루프가 절대 신경 쓰지 않는다.

체크리스트를 빠짐없이 쓰는 것 자체가 이미 개발적 사고다.
```

<br />
<br />
<br />

3. 루프는 "체크 가능한 문제"에만 통한다.

```
기계가 자동으로 맞았는지 판단할 수 있는 문제(컴파일 성공, 테스트 통과, 출력값 일치)에는 루프가 잘 먹힌다.

하지만 아래와 같은 문제는 기계가 채점할 방법이 없다.

- 이 UI가 사용자에게 직관적인가?
- 이 기능이 사업적으로 맞는 방향인가?
- 사용자가 말하지 않았지만 당연히 원했을 예외 상황

이런 문제는 사람이 직접 보고 "이거 아닌데"라고 말해줘야 루프가 방향을 튼다.
```

<br />
<br />
<br />

4. 무한 루프는 실제로는 무한하지 않다.

```
시간과 비용이 들기 때문에 "이 정도면 됐다" 또는 "막혔으니 다른 방법을 써야 한다"는 판단을 누군가 내려야 한다. 

안 그러면 같은 헛짚음을 반복하며 비용만 소모할 수 있다.
```

<br />
<br />
<br />

5. 정리

```
"무한루프를 돌리면 해결된다"는 체크 가능하고 좁게 정의된 문제에는 꽤 맞는 말이다.

다만 완전히 해결되려면 다음 두 역할이 여전히 필요하다.

- 무엇을 체크할지 정확하고 빠짐없이 정의하는 역할 (문제 정의)
- 루프가 편법을 썼는지 가끔 들여다보는 역할 (검증)

즉 인간 개입이 없어지는 것이 아니라, "직접 코드를 짜는 것"에서
"루프가 제대로 가고 있는지 감독하는 것"으로 개입의 위치가 이동하는 것이다.

개발자의 역할은 개발뿐만 아니라 책임을 진다는 것까지 포함이라는 걸 잊으면 안 된다.
```
